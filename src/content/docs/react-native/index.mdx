---
title: React Native SDK
description: Start here for Wallet UI React Native package selection, setup, and API docs.
---

import { CardGrid, LinkCard } from '@astrojs/starlight/components';

Wallet UI ships two React Native package variants. The docs below share the common React Native guidance and call out `@wallet-ui/react-native-kit` and `@wallet-ui/react-native-web3js` differences inline where they matter.

Start here if you want to choose the right package once, follow a shared mobile setup flow, and only drop into package-specific details where the APIs truly differ.

## Choose a package

<CardGrid stagger>
	<LinkCard
		href="./getting-started"
		title="@wallet-ui/react-native-kit"
		description="Use the SDK built on top of `@solana/kit`, with shared docs and synced Kit tabs for variant-specific examples."
	/>
	<LinkCard
		href="./getting-started"
		title="@wallet-ui/react-native-web3js"
		description="Use the SDK built on top of `@solana/web3.js`, with shared docs and synced Web3.js tabs for variant-specific examples."
	/>
</CardGrid>

If your app already uses `@solana/kit`, choose the Kit package. If it already uses `@solana/web3.js`, choose the Web3.js package and keep the rest of your Solana client layer consistent.

## Shared docs

<CardGrid stagger>
	<LinkCard
		href="./getting-started"
		title="Getting started"
		description="Choose a package, review template availability, and start with the shared Expo setup."
	/>
	<LinkCard
		href="./core-concepts"
		title="Core concepts"
		description="Review the shared architecture and the kit vs. web3.js client model differences."
	/>
	<LinkCard
		href="./guides/expo"
		title="Expo guide"
		description="Follow the shared React Native + Expo setup with package-specific code examples."
	/>
	<LinkCard
		href="./hooks/"
		title="Hook references"
		description="Browse shared hook references with kit and web3.js API tables and examples."
	/>
	<LinkCard
		href="./providers/"
		title="Provider reference"
		description="Review `MobileWalletProvider` props and setup for both package variants."
	/>
</CardGrid>
