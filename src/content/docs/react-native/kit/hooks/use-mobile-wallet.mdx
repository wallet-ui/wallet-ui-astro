---
title: useMobileWallet
description: Redirects to the consolidated React Native useMobileWallet docs.
---

This content has moved to the shared [`useMobileWallet`](/react-native/hooks/use-mobile-wallet/) page.

Use the `Kit` section there for the `client`, `getTransactionSigner()`, and `sendTransaction()` details.
