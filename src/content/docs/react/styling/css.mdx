---
title: Plain CSS
description: Using Wallet UI with plain CSS.
---

import PackageManagerTabs from '../../../../components/docs/PackageManagerTabs.astro';

The `@wallet-ui/css` package provides a pre-built CSS file with a default theme for the components. This is a great option if your project doesn't use Tailwind CSS.

### Installation

<PackageManagerTabs
	npm="npm install @wallet-ui/css"
	pnpm="pnpm add @wallet-ui/css"
	bun="bun add @wallet-ui/css"
/>

### Usage

You need to import the CSS file in your application.

```js
import '@wallet-ui/css/index.css';
```
